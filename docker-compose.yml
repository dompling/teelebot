version: "3.8"
services:
  teelebot:
    image: ghcr.io/plutobell/teelebot:latest
    container_name: teelebot
    restart: always
    volumes:
      - $PWD/config:/config
      - $PWD/plugins:/plugins
    tty: true
    stdin_open: true
